Deploy only — no code changes.

Action: call `supabase--deploy_edge_functions` with `["match-videos", "process-kalodata", "process-kalodata-products"]`. Report deploy result. No file edits, no config changes.